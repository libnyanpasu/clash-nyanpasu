node_modules
.DS_Store
dist
dist-ssr
*.local
update.json
scripts/_env.sh

.eslintcache
.stylelintcache

tauri.nightly.conf.json
tauri.preview.conf.json

.idea

*.tsbuildinfo


manifest/site/updater/*
!manifest/site/updater/.gitkeep
/backend/tauri/gen/

# AI agent tooling — exclude local/session state, keep shared docs & config
# (CLAUDE.md / AGENTS.md and .claude shared config stay tracked;
#  .serena manages its own cache via .serena/.gitignore)
.ccg/
.claude/settings.local.json
